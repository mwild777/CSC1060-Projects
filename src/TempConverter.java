public class TempConverter {
    public static void main(String[] args) {
        //promt user to tell if the temp they wish to convert is F or C or K
        String Type = ????;
        //Ensure user enters valid prompt
        if Type does not equal;
        //prompt user to input a tempetature
        double InitialTemp = ????;
        //convert C
        if Type = C;
        //Convert F
        else if Type = F;
        //Convert K
        else if Type = K{

        }
    }
}
