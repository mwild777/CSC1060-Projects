public class Main {
    public static void main(String[] args) {

        String fruit = "banana";
        fruit.substring(0);
    }
}
/*
relationship operators
    == equals (checks memory adress)
    != does not equal
    > lesser than
    >= lesser or equal
    < greater than
    <= greater or equal
    boolean true or false

condition statements
    if
    else
    else if

logical operators
    || or
    && and
    ! not

compare stings
    *firstString*.equals(*secondString*)
*/
